export type RegistrationRecapListData = {
  id: string;
  name: string;
  apply_date: Date;
  email: string;
  stream: string;
  topic: string;
  description: string;
  interview_date: Date | null;
  status: string;
};
